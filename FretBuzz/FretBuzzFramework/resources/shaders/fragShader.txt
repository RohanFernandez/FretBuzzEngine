#version 330 core

layout(location = 0) out vec4 color;

in vec2 TexCrd;
uniform sampler2D textureSampler;

void main()
{
	vec4 pixelColor = texture(textureSampler,TexCrd);
	if(pixelColor.a < 0.1)
        discard;

	color = pixelColor;
}